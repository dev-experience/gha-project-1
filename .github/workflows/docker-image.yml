name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  # pull_request:
  #   branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    # runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - run: echo $(date +%s)
    - name: Build the Docker image
      run: docker build ./src/TicketlessFlowApp --file Dockerfile --tag ticketless-flow-app:$(date +%s)
